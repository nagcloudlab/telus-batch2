name: 01 - Hello CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello from GitHub Actions 🚀"

      - name: Show runner info
        run: |
          uname -a
          echo "User: $(whoami)"
          echo "PWD: $(pwd)"

      - name: Show repo files (checkout NOT used yet)
        run: ls -la

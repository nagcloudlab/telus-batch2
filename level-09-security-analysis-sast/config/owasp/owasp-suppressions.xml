<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
    <!--
        OWASP Dependency-Check Suppressions for NPCI Transfer Service
        
        Suppress known false positives or accepted risks
        
        Format:
        <suppress>
            <notes>Reason for suppression</notes>
            <gav regex="true">group:artifact:version</gav>
            <cve>CVE-YYYY-NNNNN</cve>
        </suppress>
    -->
    
    <!-- Example: Suppress specific CVE if it's a false positive -->
    <!--
    <suppress>
        <notes>
            False positive: CVE-2023-12345 only affects versions before 1.0.
            We're using 2.0+ which is not affected.
        </notes>
        <gav regex="true">com\.example:library:.*</gav>
        <cve>CVE-2023-12345</cve>
    </suppress>
    -->
    
    <!-- Example: Suppress dev/test dependencies -->
    <!--
    <suppress>
        <notes>
            H2 database is only used for testing, not in production.
            Risk accepted for test environment.
        </notes>
        <gav regex="true">com\.h2database:h2:.*</gav>
    </suppress>
    -->
    
    <!-- No suppressions configured yet -->
    <!-- Add suppressions here as needed after reviewing scan results -->
    
</suppressions>
